{
  "name": "assistente-ia-worker",
  "description": "Backend do Assistente IA no Cloudflare Workers — verifica login do Firebase, lê a empresa no Firestore e chama a API da Anthropic com segurança",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "wrangler": "^4.31.0"
  }
}
